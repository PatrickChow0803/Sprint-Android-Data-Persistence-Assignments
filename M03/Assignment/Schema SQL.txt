CREATE TABLE Players(
  first_name TEXT,
  last_name TEXT, 
  age INTEGER,
  id INTEGER PRIMARY KEY
);

CREATE TABLE Teams(
  name TEXT,
  wins INTEGER,
  losses INTEGER
 );
 
INSERT INTO PLAYERS(first_name, last_name, age, id) 
VALUES ("Patrick","Chow", 22, 1);

INSERT INTO PLAYERS(first_name, last_name, age, id) 
VALUES ("Kevin","Shaw", 32, 2);

INSERT INTO PLAYERS(first_name, last_name, age, id) 
VALUES ("Bryan","Law", 12, 4);